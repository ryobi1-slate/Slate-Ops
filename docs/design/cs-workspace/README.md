# CS Workspace Design Reference

Approved future-state design for the combined CS Workspace + Queue page.

Use this as the visual and interaction reference for the staged CS Workspace Beta build.

Rules:
- Do not enqueue these files in WordPress.
- Do not copy exported CSS directly into production CSS.
- Do not remove the existing Workspace or Queue tabs during Phase 1.
- Use the design as reference only. Production work must be built inside the Slate Ops PHP, REST, JS, and CSS structure.
